//Insertion sort class

class InsertionSort_SoA {
    constructor(private array: number[] = []) {}
    public sort = (array: number[]) => {
        return array
    }
}

export default InsertionSort_SoA;