//Selection sort class

class SelectionSort_SoA {
    constructor(private array: number[] = []) {}
    public sort = (array: number[]) => {
        return array
    }
}

export default SelectionSort_SoA;