import React, { FC } from 'react';


const InsertionSort: FC = () => {
    return (
        <React.Fragment>
            <h1>Insertion sort</h1>
        </React.Fragment>
    );
};

export default InsertionSort;